"""Workshop 3: build a neural network that reads handwritten digits."""

import torch
import torchvision


torch.manual_seed(0)


# First we will build one learned matrix multiplication.


# Then we will add a ReLU bend and stack the complete network.


# Finally we will load MNIST and teach the network by repeating our loop.


def main():
    print("Your workshop file is ready. Follow the slides to build the network.")


if __name__ == "__main__":
    main()
